%%-*-latex-*-

\documentclass[11pt]{article}

\usepackage{pst-tree,pst-eps}

\input{commands}

\begin{document}

\TeXtoEPS
\pstree[nodesep=2pt,levelsep=17pt,treesep=14pt]{\TR{\fun{flat}}}{
  \pstree{\TR{\texttt{|}}}{
    \pstree{\TR{\texttt{|}}}{
      \TR{\(x\)}
      \TR{\(s\)}
    }
    \TR{\(t\)}
  }
}
\endTeXtoEPS

\end{document}
